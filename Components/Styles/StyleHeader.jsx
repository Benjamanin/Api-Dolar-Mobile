import { StyleSheet } from "react-native";

const HeaderStyles = StyleSheet.create({
    container: {
        marginTop: 100,
        alignItems: 'center',
        marginLeft: 15,
    },
    texto: {
        fontSize: 24,
        fontWeight: 'bold',
        textAlign: 'center',       
    },
});
export default HeaderStyles;